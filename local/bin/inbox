#!/bin/sh

uoa="$(mu find maildir:'/uoa/Inbox' flag:unread | wc -l)"

perso="$(mu find maildir:'/perso/Inbox' flag:unread | wc -l)"

refile="$(emacsclient -e '(my/count-refile-items)' 2>/dev/null | tr -d '\n' || echo '-')"

echo "u-$uoa  p-$perso r-$refile"
