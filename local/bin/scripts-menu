#!/bin/sh

# Choose one of the scripts in ~/.local/bin with fuzzel and run it.

CHOICE=$(find -L ~/.local/bin -maxdepth 1 -type f -executable -printf '%f\n' \
  | sort | fuzzel --dmenu)

[ -n "$CHOICE" ] && exec "$HOME/.local/bin/$CHOICE"
